#!/usr/bin/env bash
# Quick git add, commit, and pushing

git add .
if [ "$1" == "-f" ]; then
  git commit -m \""$2"\"
  git push -A
else
  git commit -m '$@'
  echo "Would you like to push? [y/N]"
  read response
  if [ "$response" == "Y" -o "$response" == "y" ]; then
    git push -A
  fi
fi



